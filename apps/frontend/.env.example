# Google OAuth Client ID — create one at https://console.cloud.google.com/apis/credentials
# Make sure to enable the Gmail API and add http://localhost:3000 as an authorized origin
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Backend proxy URL
NEXT_PUBLIC_PROXY_BASE_URL=http://localhost:8080
